name: eslint
on:
  pull_request:
  push:
    branches:
      - dev
jobs:
  eslint:
    name: Reviewdog ESLint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4.2.2
      - uses: actions/setup-node@v4.1.0
        with:
          node-version-file: './registration-app/package.json'
      - name: Install dependencies
        run: npm install ./registration-app
      #- run: npm run lint
      #  working-directory: ./registration-app

      - uses: reviewdog/action-eslint@v1.32.0
        with:
          reporter: github-check
          workdir: "./registration-app"
